<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spacex.www.mybatis.mapper.EmpMapper">

    <!--
        resultMap：设置自定义映射
            id：表示自定义映射的唯一标识
            type：查询的数据要映射的实体类的类型
        子标签：
            id：设置主键的映射关系
            result：设置普通字段的映射关系
            association：设置多对一的映射关系
            collection：设置一对多的映射关系
        属性：
            property：设置映射关系中实体类中的属性名
            column：设置映射关系中表中的字段名
			jdbcType :
    -->
    <resultMap id="EmpMap" type="Emp">
        <id property="eid" column="eid"></id>
        <result property="empName" column="emp_name"></result>
        <result property="age" column="age"></result>
        <result property="sex" column="sex"></result>
        <result property="email" column="email"></result>
    </resultMap>

    <resultMap id="empAndDeptResultMapOne" type="Emp">
        <id property="eid" column="eid"></id>
        <result property="empName" column="emp_name"></result>
        <result property="age" column="age"></result>
        <result property="sex" column="sex"></result>
        <result property="email" column="email"></result>
        <result property="dept.did" column="did"></result>
        <result property="dept.deptName" column="dept_name"></result>
    </resultMap>

    <resultMap id="empAndDeptResultMapTwo" type="Emp">
        <id property="eid" column="eid"></id>
        <result property="empName" column="emp_name"></result>
        <result property="age" column="age"></result>
        <result property="sex" column="sex"></result>
        <result property="email" column="email"></result>

        <!--
            association:处理多对一的映射关系
            property: 需要处理多对一关系的属性名
            javaType: 该属性的类型
        -->
        <association property="dept" javaType="Dept">
            <id property="did" column="did"></id>
            <result property="deptName" column="dept_name"></result>
        </association>
    </resultMap>


    <resultMap id="empAndDeptByStepResultMap" type="Emp">
        <id property="eid" column="eid"></id>
        <result property="empName" column="emp_name"></result>
        <result property="age" column="age"></result>
        <result property="sex" column="sex"></result>
        <result property="email" column="email"></result>
        <!--
            association:处理多对一的映射关系
            select: 设置分布查询的sql的唯一标识（namespace.SQLId或者mapper接口的全限定类名.方法名）
            column: 设置分布查询的条件
            fetchType: 当开启了全局的延迟加载过后，可以通过该属性手动控制延迟加载的效果
            fetchType="eager|lazy" eager：立即加载 lazy: 延迟加载
        -->
        <association property="dept"
                     select="com.spacex.www.mybatis.mapper.DeptMapper.queryEmpAndDeptByStepTwo"
                     column="did"
                     fetchType="eager">
        </association>
    </resultMap>

    <!--List<Emp> queryAllEmp();-->
    <select id="queryAllEmp" resultMap="EmpMap">
        select * from t_emp
        <!--select eid,emp_name empName,age,sex,email from t_emp-->
    </select>

    <!-- Emp queryEmpAndDept(@Param("eid")Integer eid);-->
    <select id="queryEmpAndDept" resultMap="empAndDeptResultMapTwo">
        select *
        from t_emp e
                 left join t_dept d on d.did = e.did
        where eid = #{eid}
    </select>

    <!-- Emp queryEmpAndDeptByStepOne(@Param("eid")Integer eid);-->
    <select id="queryEmpAndDeptByStepOne" resultMap="empAndDeptByStepResultMap">
        select *
        from t_emp
        where eid = #{eid}
    </select>

    <!-- List<Emp> queryDeptAndEmpByStepTwo(@Param("did")Integer did);-->
    <select id="queryDeptAndEmpByStepTwo" resultMap="EmpMap">
        select * from t_emp where did=#{did}
    </select>
</mapper>