<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spacex.www.mybatis.mapper.SqlMapper">

    <!--List<User> queryUserInfoByUserName(@Param("userName") String userName);-->
    <select id="queryUserInfoByUserName" resultType="User">
        <!--select * from t_user where username like '%${userName}%'-->
        <!--select * from t_user where username like concat('%',#{userName},'%')-->
        select * from t_user where username like "%"#{userName}"%"
    </select>

    <!--  int deleteUser(@Param("ids") String ids);-->
    <delete id="deleteUser">
        delete from t_user where id in (${ids})
    </delete>

    <!--List<User> queryUSerByTableName(@Param("tableName") String tableName)-->
    <select id="queryUSerByTableName" resultType="User">
        select * from ${tableName}
    </select>

    <!--
        int insertUser (User user);
        useGeneratedKeys :设置当前标签中的sql使用自动递增的主键
        keyProperty：将自动递增的主键赋值给传输到映射文件中参数的某个属性
    -->
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        insert  into t_user values(null,#{userName},#{password},#{age},#{sex},#{email})
    </insert>

</mapper>